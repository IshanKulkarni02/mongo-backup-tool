# Go build artifacts
/mongobak
/mongobak.exe
/dist/
*.test
*.out

# Wails desktop app (added in later phases)
# Only the compiled output is ignored — desktop/build/ also holds tracked
# source (Info.plist, the NSIS installer script, packaging scripts).
desktop/build/bin/
desktop/frontend/dist/
desktop/frontend/node_modules/

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
